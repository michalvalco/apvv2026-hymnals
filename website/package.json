{
  "name": "tepalit-companion",
  "version": "0.1.0",
  "description": "TEPALIT Digital Companion — Interactive DH website for dogmatic comparison of Lutheranism and Czech confessions",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "validate-data": "node scripts/validate-data.mjs",
    "build": "node scripts/validate-data.mjs && astro build && pagefind --site dist --output-subdir pagefind",
    "preview": "astro preview",
    "astro": "astro"
  },
  "license": "MIT",
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^6.0.8",
    "tailwindcss": "^3.4.19"
  },
  "overrides": {
    "@astrojs/tailwind": {
      "astro": "$astro"
    }
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
